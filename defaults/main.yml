---
nrpe_allowed_hosts:
  - 127.0.0.1
  - 172.29.70.2
nrpe_group: "nrpe"
nrpe_include_directory: "/etc/nrpe.d/"
nrpe_pid_file: "/var/run/nagios/nrpe.pid"
nrpe_plugins_directory: "/home/nrpe/bin/"
nrpe_port: 5666
nrpe_server_address: "{{ ansible_default_ipv4.address }}"
nrpe_user: "nrpe"

# list of groups nrpe should belong to
#nrpe_additional_groups:
#  -

# url of repository containing nrpe checks to download
#nrpe_checks_repository: https://git.example.com/nrpe_checks.git

# list of nrpe checks
#nrpe_check_list:
#  - command[check_ntp]=/home/nrpe/bin/check_ntp
